namespace ConsoleApp
{
    /// <summary>
    /// contains enumerated types login, logout, register, exit
    /// </summary>
    public enum Redirect
    {
        exit = 0,
        login = 1,
        register = 2,
        ForgotPassword = 3,
        logout = 4
    }
}
